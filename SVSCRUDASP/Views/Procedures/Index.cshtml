@model IEnumerable<SVSCRUDASP.Models.Procedure>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProcedurePrice)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProcedurePrice)
        </td>
        <td>
            @Html.ActionLink("Treatments Main Page", "Index", "Treatments") |
            @Html.ActionLink("Treatments", "Treatments", new { id = item.ProcedureID }) |
        </td>
    </tr>
}

</table>
