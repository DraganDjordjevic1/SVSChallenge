@model IEnumerable<SVSCRUDASP.Models.Pet>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Owner.GivenName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PetName)
            
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PetName }) |
            @Html.ActionLink("Details", "Index", "Treatments") |
            @Html.ActionLink("Delete", "Delete", new { id = item.Type })
        </td>
    </tr>
}

</table>
